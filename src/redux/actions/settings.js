import { OPEN_USER_DIALOG, CLOSE_USER_DIALOG } from '../action-types/settings';

export const openUserDialog = {
  type: OPEN_USER_DIALOG,
};

export const closeUserDialog = {
  type: CLOSE_USER_DIALOG,
};
